"""
Weight utilities:
    - Validate weight dictionaries
    - Apply defaults
    - Clamp values to valid ranges
"""

DEFAULT_WEIGHTS = {
    "distance": 1.0,
    "lighting": 1.0,
    "greenery": 1.0,
    "pollution": 1.0,
}

# Ensure weights contain valid keys and numberic values
def validate_weights(weights):
    pass

# Fill missing weights with defaults
def apply_default_weights(weights):
    pass